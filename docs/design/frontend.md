# フロントエンド設計

このドキュメントでは、アプリケーションの主要なページ（画面）の構成と機能について記述します。

## ページ構成

### トップページ
- **ルーティング:** `/`
- **概要:** アプリケーションのデフォルトの初期画面です。現状では、SolidJSのテンプレートに含まれる "Hello World" メッセージが表示されます。将来的には、メディアソース一覧ページへのリダイレクトや、ダッシュボードのような機能が実装される可能性があります。

### メディアソース一覧ページ
- **ルーティング:** `/sources`
- **概要:** 登録されているメディアソースを管理する中心的な画面です。
- **実装済みの機能:**
  - 登録済みの全メディアソースをカード形式で一覧表示します。
  - 「Add Source」ボタンから、新しいメディアソースを追加するモーダルを開くことができます。
  - 各メディアソースカードから、既存のメディアソース情報を編集するモーダルを開くことができます。
  - 各メディアソースカードから、対象のメディアソースを削除する確認モーダルを開くことができます。
- **コンポーネント:**
  - `SourceCard`: メディアソースの情報を表示するカード。
  - `SourceFormModal`: メディアソースの追加・編集を行うフォームを持つモーダル。`TanStack Form` と `Zod` を使用した堅牢なバリデーションを実装。
  - `SourceDeleteModal`: メディアソースの削除を確認するモーダル。

### メディア一覧ページ
- **ルーティング:** `/sources/[mediaSourceId]`
- **概要:** 特定のメディアソースに含まれるメディアファイルを一覧表示する画面です。
- **実装済みの機能:**
  - **レスポンシブレイアウト:**
    - **デスクトップ:** 画面左側に検索フィルターサイドバーを表示。
    - **モバイル:** 画面上部のナビゲーションバー内のフィルターボタンから、検索フィルターモーダルを表示。
  - **無限スクロール:** TanStack Queryの`createInfiniteQuery`を使用し、スクロールに合わせて自動的に追加のメディアを読み込みます。
  - **データ取得:** `/api/sources/{mediaSourceId}/search` エンドポイントを使用し、ページネーション（offset/limit）による効率的なデータ取得を実現しています。
  - **スクロール位置の復元:** ページ遷移時およびリロード時にスクロール位置を復元します。`sessionStorage`を使用して位置を永続化し、リロード時は初期読み込み件数を増やすことでスムーズな復元を実現しています。
  - **リアルタイム更新:** Server-Sent Events (SSE) を使用して、ファイルの追加・削除・変更を検知し、自動的にリストを更新します。
  - メディアファイルのサムネイルをグリッドレイアウトで表示します。
  - サムネイルをクリックすると、メディア詳細ページに遷移します。
  - 画面全体がドラッグ＆ドロップのターゲットになっており、ファイルをドロップするとアップロードモーダルが開きます。
  - 画像やURLをページにペースト（貼り付け）すると、アップロードモーダルが開きます。
  - 画面右下の「＋」ボタンからファイル選択ダイアログを開き、画像またはJSONファイルをアップロードできます。
  - JSONファイルをアップロードすると、一括ダウンロード機能がトリガーされます。
- **コンポーネント:**
  - `UploadMediaModal`: ファイル名や説明などを設定してメディアをアップロードするためのモーダル。共通バリデーションスキーマを使用。
  - `SearchFilters`: 検索条件（タグ、プロジェクト、IP、キャラクターなど）を設定するUIコンポーネント。
- **技術的な実装詳細:**
  - **データ取得戦略:**
    - メディア一覧の取得には、通常のリストエンドポイントではなく、検索用エンドポイント (`/api/sources/{mediaSourceId}/search`) を使用しています。
    - **理由:** 通常のリストエンドポイントは全件取得を前提としており、大量のメディアがある場合にパフォーマンスの問題が発生するためです。検索エンドポイントはページネーション（offset/limit）をネイティブにサポートしており、無限スクロールの実装に適しています。
  - **無限ローディング問題の解決:**
    - **問題:** 以前は、APIクライアントの動的インポート（`import(...)`）が原因で、SSR/ハイドレーション時に非同期処理が正しく解決されず、ローディング状態が終わらない（無限ロード）問題が発生していました。
    - **解決策:** APIクライアントのインポートを静的インポート（トップレベルの `import`）に変更することで、SSRとクライアントサイドでのコード実行の整合性を確保し、この問題を解決しました。
- **将来の構想:**
  - ファイル名、タグ、キャラクター、IPなどでメディアを絞り込む高度な検索バーの実装（実装済み）。
  - ディレクトリ階層を示すパンくずリストの表示。
  - ファイル名、作成日時、ファイルサイズなどでの並び替え機能（実装済み）。

### メディア検索ページ
- **ルーティング:** `/search`
- **概要:** システム全体でタグベースのメディア検索を行う専用画面です。
- **実装済みの機能:**
  - **レスポンシブレイアウト:**
    - **デスクトップ:** 画面左側に検索フィルターサイドバーを表示。
    - **モバイル:** 画面上部のナビゲーションバー内のフィルターボタンから、検索フィルターモーダルを表示。
  - **メディアソース選択:** 検索対象のメディアソースを選択できます。
  - **共通フィルタコンポーネント:** `SearchFilters` コンポーネントを使用し、メディア一覧ページと統一された検索体験を提供します。
  - **タグ入力補完:** solid-uiの`Command`コンポーネントを使用し、システムに登録されているタグをオートコンプリートで選択できます。モバイルでは`Popover`を使用した軽量な表示に切り替わります。
  - **含むタグ選択:** 検索結果に含めたいタグを複数選択できます。選択されたタグはバッジとして表示され、削除可能です。
  - **除外タグ選択:** 検索結果から除外したいタグを複数選択できます。
  - **プロジェクトフィルタ:** プロジェクトによる絞り込みが可能です（複数選択可）。
  - **IPフィルタ:** IP（作品）による絞り込みが可能です（複数選択可）。
  - **キャラクターフィルタ:** キャラクターによる絞り込みが可能です（複数選択可）。
  - **タグマッチモード:** AND（すべて含む）またはOR（いずれかを含む）を選択できます。
  - **ファイル名検索:** ファイル名による部分一致検索が可能です。
  - **ソート機能:** 作成日、ファイル名、ファイルサイズで昇順・降順にソートできます。
  - **ページネーション:** 検索結果のページング機能（次へ/前へボタン、結果数表示）。
  - **検索結果表示:** レスポンシブなグリッドレイアウトで検索結果を表示し、各メディアのサムネイル、ファイル名、パス、寸法、ファイルサイズを表示します。
- **使用API:**
  - `GET /api/tags` - タグ一覧取得（オートコンプリート用）
  - `GET /api/projects` - プロジェクト一覧取得（フィルタ用）
  - `GET /api/ips` - IP一覧取得（フィルタ用）
  - `GET /api/characters` - キャラクター一覧取得（フィルタ用）
  - `GET /api/sources` - メディアソース一覧取得
  - `GET /api/sources/{mediaSourceId}/search` - メディア検索実行
- **技術スタック:**
  - solid-ui `Command`, `Popover`コンポーネント
  - `SearchFilters` 共有コンポーネント



### メディア詳細ページ
- **ルーティング:** `/sources/[mediaSourceId]/[mediaId]`
- **概要:** 一つのメディアファイルを大きく表示する画面です。
- **実装済みの機能:**
  - メディアファイル（現在は画像のみ）を画面中央に表示します。
  - **サイドバー機能:**
    - **基本情報:** ファイル名、パス、解像度、ファイルサイズを表示します。
    - **説明 (Description):** メディアの説明を表示・編集できます。Markdown形式に対応しています。
    - **ソースURL:** 元のURLがある場合はリンクとして表示します。
    - **関連付け管理:** プロジェクト、IP、キャラクターの関連付けを追加・削除・新規作成できます。
    - **タグ表示:** Positive/Negativeタグを表示します。
    - **生成情報:** AI生成画像のプロンプト、ネガティブプロンプト、ワークフロー情報を表示します。
- **将来の構想:**
  - 関連メディア（バージョン違い、差分など）の表示。
