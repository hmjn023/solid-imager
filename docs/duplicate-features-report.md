# 機能重複検知レポート

> Generated by [indexion](https://github.com/trkbt10/indexion) v0.6.0  
> 解析日: 2026-04-05  
> コマンド: `indexion plan refactor` (hybrid strategy)

---

## サマリー

| 対象レイヤー | 検知ペア数 | 最高類似度 |
|---|---|---|
| アプリケーション層 (`services/`) | 274 pairs | 93% |
| インフラ層 (`repositories/`) | 83 pairs | 94% |

---

## 🚨 クリティカル: サービス層の機能重複 (90%+)

類似度が高いほど、実装の重複・責務の混在が疑われます。

| ファイル1 | ファイル2 | 類似度 | 考察 |
|---|---|---|---|
| `media-processing-service.ts` | `media-service.ts` | **93%** | メディア処理ロジックの責務分離が不明瞭 |
| `workflow-service.ts` | `analytics-service.ts` | **92%** | ほぼ同一の骨格（analytics未実装のスタブ） |
| `backup-service.ts` | `media-service.ts` | **92%** | backupにmediaのコードが大量コピー |
| `backup-service.ts` | `tagging-service.ts` | **92%** | 同上 |
| `data-migration-service.ts` | `analytics-service.ts` | **92%** | analytics が実質スタブのため構造が酷似 |
| `bidirectional-sync-service.ts` | `media-service.ts` | **91%** | sync内にmedia操作が混在 |
| `backup-service.ts` | `media-processing-service.ts` | **91%** | 処理ロジックの重複 |
| `backup-service.ts` | `maintenance-service.ts` | **91%** | 同一パターン多数 |
| `media-processing-service.ts` | `bidirectional-sync-service.ts` | **90%** | 処理パイプラインの重複 |
| `media-processing-service.ts` | `tagging-service.ts` | **90%** | タグ付け処理の責務が分散 |
| `backup-service.ts` | `directory-sync-service.ts` | **90%** | sync系の重複 |
| `tagging-service.ts` | `media-service.ts` | **90%** | タグ付けロジックがmedia-serviceにも存在 |
| `directory-sync-service.ts` | `maintenance-service.ts` | **90%** | ディレクトリ走査ロジックの重複 |

### 最優先: `preset-service.ts` vs `filter-preset-service.ts`

同一ドメイン（フィルタープリセット）の実装が2ファイルに分散している可能性が高い。  
→ どちらかを削除し、一本化すること。

### 次優先: `analytics-service.ts` のスタブ問題

`"Not implemented"` が **7ファイル・23箇所** に存在。特に `analytics-service.ts` は大半が未実装。  
`workflow-service.ts` との92%類似は、骨格コピーによるものと推定。

---

## 🚨 クリティカル: リポジトリ層の機能重複 (90%+)

| ファイル1 | ファイル2 | 類似度 | 考察 |
|---|---|---|---|
| `character-repository.ts` | `ip-repository.ts` | **94%** | メディアへの関連付けロジックが同一 |
| `character-repository.ts` | `tag-repository.ts` | **94%** | 同上 |
| `media-repository.ts` | `character-repository.ts` | **94%** | CRUD骨格が同一 |
| `ip-repository.ts` | `tag-repository.ts` | **92%** | confidence/source更新SQLが重複 |
| `character-repository.ts` | `collection-repository.ts` | **94%** | CRUD骨格重複 |
| `source-repository.ts` | `tag-repository.ts` | **92%** | エラーハンドリング重複 |

### 特定: `authors-repository.ts` vs `author-repository.ts` (82%)

**同一リポジトリの二重実装**と思われる。どちらかが廃止されるべき。

---

## ⚠️ コードレベルの重複

### エラーハンドリングパターン (全リポジトリ)

以下のコードブロックが **15箇所以上** に重複:

```typescript
} catch (error) {
    if (error instanceof ResourceNotFoundError) {
        throw error;
    }
    // ... UnexpectedError へのラップ
}
```

```typescript
} catch (error: unknown) {
    if (
        error &&
        typeof error === "object" &&
        "code" in error &&
        error.code === "23505"
    ) {
        throw new ResourceConflictError(...);
    }
}
```

**推奨対応:** `apps/server/src/infrastructure/repositories/shared/` に共通エラーハンドラーを抽出。

### `media-repository.ts` 内の関数重複

| 関数1 | 関数2 | 類似度 |
|---|---|---|
| `buildFolderCondition` | `buildCriterionQuery` | 81% |
| `buildKeywordCondition` | `buildCriterionQuery` | 76% |

→ パラメータ化した単一関数に統合可能。

### `character-repository.ts` / `ip-repository.ts` の `confidence`/`source` 更新SQL

```typescript
set: {
    confidence: confidenceUpdateSql,
    source: sourceUpdateSql,
    // ...
}
```

この SQL パターンが `character-repository.ts:343,412`、`ip-repository.ts:161,215`、`tag-repository.ts:263` に重複。  
→ 共通 SQL ビルダー関数に抽出推奨。

---

## 📋 マジックストリング（定数化候補）

### サービス層

| 文字列 | 出現ファイル数 | 出現回数 | 対応 |
|---|---|---|---|
| `"Not implemented"` | 7 | 23 | 定数化 or 未実装サービスを整理 |
| `"processMedia"` | 5 | 17 | ジョブ名を定数に |
| `"local"` | 7 | 16 | ストレージタイプ enum 化 |
| `"Media source not found"` | 3 | 8 | エラーメッセージ定数化 |
| `"auto_tagging"` | 2 | 2 | ジョブ名定数化 |
| `"comfyui_workflow"` | 2 | 2 | タグソース enum に統合 |
| `"media-deleted"` | 2 | 2 | イベント名定数化 |

### リポジトリ層

| 文字列 | 出現ファイル数 | 出現回数 | 対応 |
|---|---|---|---|
| `"23505"` | 6 | 14 | PostgreSQL エラーコード定数 |
| `` "`excluded.source`" `` | 3 | 5 | SQL テンプレート関数化 |
| `` "`excluded.confidence`" `` | 3 | 5 | 同上 |
| `"manual"` | 3 | 12 | タグソース enum に統合 |

---

## 🔧 推奨アクション（優先順）

### P0: 即時対応

- [ ] `preset-service.ts` と `filter-preset-service.ts` の統合または役割分担の明確化
- [ ] `authors-repository.ts` と `author-repository.ts` のどちらかを廃止

### P1: リポジトリ共通化

- [ ] `apps/server/src/infrastructure/repositories/shared/error-handlers.ts` を作成
  - `handleNotFoundError()` — `ResourceNotFoundError` 再スロー + ラップ
  - `handleUniqueConstraintError()` — `"23505"` → `ResourceConflictError` 変換
- [ ] `character-repository.ts` / `ip-repository.ts` / `tag-repository.ts` の confidence/source 更新 SQL を共通化

### P2: サービス層リファクタリング

- [ ] `media-processing-service.ts` と `media-service.ts` の責務境界を明確化
- [ ] `backup-service.ts` から `media-service.ts` / `tagging-service.ts` への委譲に変更（コピーではなく呼び出し）
- [ ] `analytics-service.ts` の未実装メソッド（`getDuplicateMedia`, `getSimilarMedia`）を実装 or 削除

### P3: 定数整理

- [ ] ジョブ名 (`"processMedia"`, `"auto_tagging"`) を `packages/core` の定数ファイルに集約
- [ ] `"23505"` を `PG_UNIQUE_VIOLATION = "23505"` として定数化
- [ ] `"local"`, `"positive"`, `"negative"`, `"manual"` を既存の enum/union type に統合

### P4: `media-repository.ts` 内部整理

- [ ] `buildFolderCondition` / `buildKeywordCondition` / `buildCriterionQuery` を統合

---

## 実行コマンド

```bash
# 再実行方法 (indexion v0.6.0)
export PATH="$HOME/.indexion/bin:$PATH"

# サービス層
indexion plan refactor apps/server/src/application --include "**/*.ts" --threshold 0.7 -o docs/refactor-services.md

# リポジトリ層  
indexion plan refactor apps/server/src/infrastructure/repositories --include "**/*.ts" --threshold 0.75 -o docs/refactor-repositories.md
```
