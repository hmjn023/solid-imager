interface:
  display_name: "Job System"
  short_description: "solid-imagerのbackground job設計・worker実装規約"
  default_prompt: "Use $job-system to follow existing worker, dispatch, concurrency, and batch progress rules."
