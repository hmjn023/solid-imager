interface:
  display_name: "Media Search"
  short_description: "solid-imagerの検索状態・API・共有UI設計規約"
  default_prompt: "Use $media-search to follow the existing search store, session persistence, API, and shared UI design."
